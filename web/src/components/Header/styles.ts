import styled from 'styled-components';

export const Container = styled.div`
  .cart-button {
    position: relative;
    float: right;
    z-index: 10;
    margin-right: 20px;
    margin-top: -5.5%;
  }
`;
